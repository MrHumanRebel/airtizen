# AirTizen icon assets

These launcher/taskbar icons are resized derivatives of the user-provided `airtizen_logo.png` logo. They are not regenerated artwork.

Tizen launcher icon used by `config.xml`:

```xml
<icon src="icon.png"/>
```
