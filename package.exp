#!/usr/bin/expect -f
set timeout -1
spawn ./tizen-studio/tools/ide/bin/tizen package -t wgt -o ./app/release -s AirTizen -- ./app/.buildResult
expect {
  "Author password:" { send -- "1234
"; exp_continue }
  "Yes: (Y), No: (N) ?" { send -- "Y
"; exp_continue }
  eof
}
